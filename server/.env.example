# GPU Server Configuration
# ============================================================================
# Copy this file to .env and update with your values
# ============================================================================

# API Authentication
# IMPORTANT: Change this to a secure random key in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
GPU_API_KEY=your-super-secret-api-key-here

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Workspace (where job files are stored)
WORKSPACE_ROOT=/workspace/jobs

# Python environment path (where nerfstudio is installed)
NERF_PYTHON_PATH=/opt/miniforge3/envs/nerf/bin/python

# Optional: Callback settings
# ENABLE_CALLBACKS=true
